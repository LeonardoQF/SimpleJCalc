name: java-ci

on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main

jobs:
    test:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout the code
              uses: actions/checkout@V7
            
            - name: Set Java up
              uses: actions/setup-java@v6
